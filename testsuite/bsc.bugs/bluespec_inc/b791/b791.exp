if {$vtest == 1} {
  compile_verilog_pass loop.bsv
  link_verilog_pass loop.v loop
}

# mkTuple has a Tuple2 interface.  A tuple is not a valid module interface
# (PrimPair is a struct, not an interface), so synthesizing it fails (T0043).
compile_verilog_pass_bug Bug791.bsv mkTuple tuple-interface
