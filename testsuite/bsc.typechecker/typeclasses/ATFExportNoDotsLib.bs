package ATFExportNoDotsLib (Container, Box(..)) where

-- Library that exports Container WITHOUT (..) — ATF Elem should NOT be visible.

class Container f e | f -> e where
  type Elem f = e
  extract :: f -> e

data Box a = Box a

instance Container (Box a) a where
  extract (Box x) = x
