package ATFExportLib (Container(..), Box(..)) where

-- Library that exports Container with (..) — ATF Elem should be visible.

class Container f e | f -> e where
  type Elem f = e
  extract :: f -> e

data Box a = Box a

instance Container (Box a) a where
  extract (Box x) = x
